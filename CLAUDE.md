# Steel Rampage

Twisted Metal-style arena vehicular combat game for the web. Single-player vs AI bots today; architected for online multiplayer later.

## Commands

- `npm run dev` — Vite dev server (port 5173). Also launchable via `.claude/launch.json` (name: `steel-rampage`).
- `npm run build` — typecheck (`tsc`) + production build.
- `npx tsc --noEmit` — typecheck only.

## Stack

Three.js (rendering) + Rapier `@dimforge/rapier3d-compat` (physics, WASM) + Vite + TypeScript. No external assets — cars, arena, textures, particles, and audio are all procedural.

## Architecture

The simulation is deliberately separated from rendering so server-authoritative netcode can slot in later:

- `src/main.ts` — bootstrap, menu, fixed-timestep loop (60 Hz sim via accumulator, per-frame render).
- `src/game/game.ts` — match coordinator: weapons (hitscan MG, homing missiles), damage/kills/respawns, lock-on targeting, ram damage via Rapier collision events, camera (chase + occlusion raycast + shake), win condition. `step(dt, input)` is the sim; `render(dt)` is visuals-only.
- `src/game/vehicle.ts` — raycast-suspension arcade car physics. Key design: **yaw-rate controller** (steering commands a target yaw rate; torque drives toward it) instead of relying on tire forces alone — this is what makes handling feel tight and prevents spin-outs. Suspension/grip/drive forces are applied per-wheel as impulses scaled by body mass.
- `src/game/bots.ts` — AI: target selection (grudge > player-bias > nearest), obstacle-avoidance ray, unstuck reverse, burst fire, pickup-seeking when low.
- `src/game/arena.ts` — physics colliders + meshes for the arena, spawn points, pickup locations, lighting.
- `src/game/specs.ts` — car archetype tuning table (speed/armor/grip per car).
- `src/render/` — procedural car meshes (box-built per archetype), particle/tracer/explosion effects.
- `src/audio/sfx.ts` — procedural WebAudio SFX (no audio files). Init requires user gesture (start button).
- `src/ui/hud.ts` — DOM-based HUD: bars, scoreboard, kill feed, radar canvas, hit feedback.

## Conventions & gotchas

- Car forward is **-Z**; steer input positive = left. Yaw quaternion helpers in `vehicle.ts`.
- All gameplay forces are impulses scaled by `dt` and body mass — never per-frame forces.
- Rapier ray hits: use `(hit as any).timeOfImpact ?? (hit as any).toi` (API renamed across versions).
- `window.__game` is a debug handle set on match start — used for headless testing via `game.step(1/60, mockInput)`.
- Vehicles get 3s spawn protection; firing cancels it.
- The preview browser tab throttles rAF when backgrounded — test physics by manually stepping `__game.step(...)` in an eval, never by wall-clock waiting.

## Roadmap (agreed with user)

Phase 1 (done): arena deathmatch vs 5 bots, 3 car classes, MG + homing missiles + pickups, first-to-15.
Phase 1.5 (done): 3 game modes (deathmatch / survival 3-lives / time attack 3min), mines + shield + overdrive power-ups, explosive chain-reaction barrels, overpass + tunnel arena, detailed car models, pause menu. Physics: grip applied at CoM height + speed-scaled anti-roll (no turbo-turn flips); missiles launch low + proximity fuse (no point-blank pass-through).
Phase 1.6 (done): 6-vehicle roster, each with a unique special weapon on an energy meter (VIPER nitro-ram dash, HELLCAT twin miniguns, SCORCH flamethrower, RAMPART 360° auto-turret, JUGGERNAUT seismic slam, MORTIS remote bomb — `specialId` in specs.ts, engine in game.ts `handleSpecial`). MG is the only default weapon: everyone starts with 0 missiles/0 mines (pickups only). Controls: RCLICK/E special, G missile, Q mine.
Phase 1.7 (done): special input is EDGE-TRIGGERED (`input.consumeSpecial()`) — a held button must never re-trigger (it used to insta-detonate MORTIS's just-launched bomb). Town arena: crossing N-S/E-W streets with road/facade/sign canvas textures, freeway overpass at the intersection (N-S road bridges the E-W street — ramp HIGH ends must face the deck), gas station with barrel cluster, billboards, streetlights, wrecks. Cars rebuilt as beveled ExtrudeGeometry side-profiles (see carMesh.ts `extrudeProfile`; profile +x = car front = world -Z).
Phase 1.8 (done): real GLB car models (Kenney Car Kit, CC0, in `public/models/` — bodies + separate wheel models mounted on our steer/spin rig; loader in `render/carModels.ts`). Per-vehicle paint via palette recoloring (saturated pixels take spec.color; police/ambulance/taxi keep stock livery — `KEEP_STOCK_PAINT`). GLBs reference external `Textures/colormap.png` — must ship alongside. 8-vehicle roster (added MEDIC ambulance 'repair' + JACKRABBIT taxi 'minetrail'). Shield now blocks 100% of damage. Missiles hard-capped at 3 (full rack refuses pickup, leaves it on the ground). Cars ground via soft AO contact blob (no underglow quad). Turbo pickup is a thunderbolt.
Phase 1.9 (done): "Overdrive City" arena 240×240 (docs/LEVEL-DESIGN.md) — octagonal ring highway (20m, 10× vehicle width), 18m boulevards, ≥13m alleys, anti-snag props (`addProp` = roundCuboid at 92% of visual), encounter-cadence formula for map sizing. Pedestrians (peds.ts, kinematic, flee AI) grant +4 armor at ≥12 m/s with 2.5s per-vehicle cooldown. Pickup respawns: ±30% jitter + 12m proximity hold (anti-spawn-camping) + overdrive roams 3 sockets. Balance system (docs/BALANCE.md): universal 100 HP pool, armor rating mitigation `raw×100/(100+armor)`, damage hierarchy locked missile 34 > specials ≤29 (per-activation per-victim ledger) > ram ≤18 > MG 2.2. Lock-on: 0.9s cone acquisition (36.9°/78m), 3.0/s decay, hysteresis retention, LOCKING ring → LOCKED + sfx.
Phase 1.10 (done): 80% vehicle scale (suspension REST_LEN/WHEEL_RADIUS scaled; knockback impulses mass-normalized), speed-illusion camera (FOV 74+14·speedN², distance stretch). HUD: 10-block segmented armor bar + 10-block shield timer bar (10s full-immunity shield, final-2s expiry blink), over-vehicle enemy HP sprites (shown when locked/locking/recently-damaged), panic state <30% (vignette + quickening heartbeat sfx), aggregated damage popups (player-dealt only, 0.25s window, yellow→red under 30%).
Phase 1.11 (done): scripted loop REMOVED (user: felt out-of-control/physics-defying) → SKYWAY: drivable elevated roller-coaster track along z=-78 (`skySeg` in arena.ts — on-ramps both ends, humps tuned so ~20 m/s keeps contact & boosted speed gets air, guard rails, deck boost pad + crest pickups; boost pads have y for elevation). 9 boost pads (+10 m/s, +1.2s turbo, 1.2s/vehicle cooldown, 1.45× cap). Sky presets (sunset/day/night, random per match): gradient dome + sun disc + stars in arena.ts SKY_PRESETS.
Phase 1.12 (done): "juice" overhaul — altitude-aware AO shadows (ground-projected raycast, fade+expand with height), zone-contrast textures (dark asphalt + dense bright dashes vs warm concrete lots), horizon-matched fog + distant skyline silhouette cylinder (r300, per-preset tint), radar street-map underlay + high-value pickup dots, turbo camera (+15° FOV + pull-back via turboLerp), wall-impact shake/sparks/thud (vehicle-vs-static collision events), mid-air pitch/roll control (W=nose down; overrides self-level), perfect-landing detection (flat + airtime>0.35s → 1s boost) + heavy-landing rumble, drift skid-mark pool (240, recycled) + tire smoke, modular engine audio (sports scream / V8 rumble / rally punch per build, virtual gear shifts), missile-lock warning beeps (rate scales with distance), bass-boosted explosions. Bridge ramp yaws re-fixed after Arena 3.0 regression (high ends MUST face the deck — see addRamp).
Phase 1.12 (done): visual/spawn rigidity pass. Buildings rebuilt (`makeBuildingTextures` glass/concrete/brick albedo+normal+roughness maps; `building()` adds decorative base plinth, floor ledges, parapet cornice, corner pilasters, rooftop vents/stacks — no colliders, main box keeps full footprint). Cars get shared brushed-metal scratch roughnessMap + metalness 0.55 (`getCarScratchMap` in carMesh.ts, applied to ALL bodies incl stock-paint). Bulletproof respawn (`respawn()` in game.ts): scores on-road spawn points, HARD-invalidates any within 28m of a living enemy OR 34m of a missile heading toward it, routes to safest. Blinking invuln: player mesh visibility toggles at 9Hz while `spawnProtection > 0`. NOTE: systems 3-8 of the "9-system spec" (AO shadow, zone textures, skybox/fog, radar map, turbo FOV, screen shake, mid-air control, particles, modular audio) were ALREADY built in phases 1.9-1.11 — this phase only added the genuinely-missing texture rigidity + spawn safety.
Phase 1.13 (done): render-quality lighting pass (Blender-bridge pivot — MCP add-on wouldn't connect, so did it in-engine). PMREM reflection environment built per-match from `arena.envColors` (sky top/horizon + a bright horizon band for specular sweep on paint) → `scene.environment`, so all metal/paint/glass reflect. Car bodies: metalness 0.6 / roughness 0.5 / envMapIntensity 1.5 (carMesh.ts). Postprocessing in main.ts: EffectComposer → RenderPass → UnrealBloomPass (strength 0.62, radius 0.5, threshold 0.82) → OutputPass; `renderComposed()` replaces `renderer.render`, updated on resize. Neon/turbo/explosions/lit-windows now glow. Limiting factor for further quality is low-poly Kenney car GEOMETRY (shading is now render-technique-equivalent to Eevee).
Phase 1.14 (done): Blender asset pipeline + render pass. **The `mcp__Blender__*` tools DO NOT WORK (time out); talk to the add-on's socket directly on localhost:9876 — see docs/BLENDER.md for protocol + the modeling techniques that worked.** Custom car (`hero-body.glb`/`hero-wheel.glb`) modeled in Blender: lofted body + glass greenhouse + painted roof cap, boolean wheel arches, lights/grille, named materials — wired to the muscle archetype (HELLCAT). `carMesh.ts` recolours only map-less materials so authored glass/lights survive. Roads: raised decorative curbs (no colliders) along boulevards + ring straights, road texture at 512×1024 with aggregate grain, patches, cracks and tyre-polish lanes. Bloom threshold raised to 0.88 (0.82 blew out sunlit curbs).
Phase 1.15 (done): **the arena now lives in Blender.** `public/models/arena.glb` (765 KB, 292 objects) carries all structures + colliders + gameplay markers; `consumeArenaGLB()` in arena.ts routes by name prefix (COL_/SPAWN_/PICKUP_<type>_/BARREL_/BOOST_/PED_) and falls back to the old procedural city if the GLB is missing. Sky/fog/lights/ground/road textures/radar stay code-side. THREE CRITICAL EXPORT GOTCHAS in docs/BLENDER.md (transforms baked into vertices → use bbox centres; never transform_apply scale on rotated objects; Blender +Y → game −Z rigid rotation — skyway is now at z=+78). Verified: building collision, full skyway traversal, bridge over-deck run, all 45 markers at correct positions.
Phase 1.16 (done): **Arena 3.0 "Overdrive City XL"** — 320×320 (ARENA_HALF 160, RING_R 100, camera far 800, fog 175/530, dome r700, skyline r420). TRUE CIRCULAR ring highway: `ringRoadGeometry()` annulus with RADIAL UVs (v=arc/26, whole-number repeat → seamless wrap; **winding must face +Y** — first version was invisible from above). NFS-grade roads: `makeRoadMaps(kind)` albedo+roughness pairs (aggregate, tar-sealed cracks, oil-polished wheel tracks smoother in roughness map → specular sheen, worn paint), junction pads w/ zebra crosswalks + stop lines at centre + 4 ring crossings, curved curb segments (88/edge, gapped at junctions). Blender arena3 (`/tmp/bl/arena3.py` pattern, 384 objects, 938 KB): 22 buildings, 2 DIAGONAL NEON TUNNELS along the (1,−1) game diagonal at (±45.5,∓45.5) — tube walls/roof COL'd, cyan interior strips, portal lintels+glow, mines pickup + boost pad inside; skyway moved to z=+120 (x −90..90); ring streetlights every 30°; 8 ring spawns tangent-oriented. Tunnel FLOORS are code-side asphalt strips (rotZ −π/4). Radar rebuilt: circle+cross+tunnel diagonals+skyway, RANGE must equal buildRadarMap scale (160). Verified: tunnel drive-through + walls hold, bridge run, marker counts (8/20/10/9/8), ring visible+textured.
Phase 1.17 (done): Arena 3.0 bug sweep (user: "lots of bugs"). Found via live 60-90s bot soaks + settle-probes, ALL verified fixed: (1) bots stuck on tunnel walls/buildings — bots.ts rewritten: 3-whisker always-on avoidance (±29°, persistent 0.55s bias — single speed-gated ray oscillated on long oblique walls), reverse-out steers toward the OPEN side, 4s-blocked pickup goals dropped + 7s ban, repeated unstuck → 45m sideways detour (frustration counter, decays). (2) buildings at (±48,±126) stood THROUGH the skyway corridor (deck z 115-125) — moved to ±136; symptom was car pinned mid-deck at x=40, diagnosed by settle-probing surface heights (15.7 ceiling = building top). (3) overdrive pickup sat ON RampN (z=24 = ramp top) → moved to z=32; roam alts were old coords INSIDE tunnel bores → (±42,±42) plazas. (4) shield pickups placed exactly INSIDE bunker wall boxes → moved to L-pockets (±134.5,∓13.5 blender). (5) interchange invisible at night (dark M_PLAT/M_RAMP ≈ asphalt) → brightened + emissive cyan edge rails on bridge ramps. (6) skyway guard rails 0.6m were beachable → 1.1m. Soak result: 5/5 bots 692-1802m per 90s, kills flowing. Bot-travel soak + surface settle-probe are the go-to arena regression tests.
Phase 1.18 (done): **Arena 4.0 "Sunbaked Junction"** — organic TM-small-town redesign (user refs: TM town screenshot + map blueprint). Layout: central ROUNDABOUT (r22 w13, drivable pavers island r15.5, brick CLOCK TOWER landmark w/ 4 faces + COL) + main avenue on the (1,−1) diagonal running THROUGH both neon tunnels + cross avenue (1,1) + connector streets x/z=±88 (threaded between tunnel portals ≤69 and skyway pylons at 77) + rounded-rect perimeter loop w/ 4 arc corners (r30 at ±108,±108). `STREETS`/`ARCS`/`ROUNDABOUT` exported from arena.ts = single source of truth, radar draws from them (can't drift). `arcRoadGeometry` generalizes the radial-UV annulus to partial arcs. All street crossings coincide at 4 diagonal junctions (±88,±88) + 8 perimeter T-pads. Mood: 'sunbaked' sepia preset (50% weighted) + rolling-hills silhouette w/ water towers + warm ground/curbs. Blender arena4 (323 objects): 6 warm building variants (brick/cream/terra, shop awnings, rooftop water towers), 19 buildings + 4 town-square shops at (0,±36),(±36,0), gas at game (60,115), bunkers (±116,±16), interchange REMOVED (roundabout replaced it). Verified: avenue-through-tunnel run, island drivable + tower blocks, 5/5 bots 865-1734m/90s.
Phase 1.19 (done): vehicle weaponization pass (user refs: Twisted Metal 2012 — armed + battle-worn). carMesh.ts `buildFromModel` now mounts a full kit from the body bbox (roofY/hoodY): armored MG housing w/ muzzle brakes + ammo box, whip antennas, emissive taillight strips, license plate, side armor skirts on ALL cars; shared builders `gatling()` (6-barrel cluster + ammo drum), `lightRack()` (emissive lamps, police colors on RAMPART/MEDIC), `windowBars()` (armor cage), `exhaustStacks()`, `plowBlade()` (ribbed + angled wings). Per-build loadouts: muscle hood pods+rear cage+spoiler, suv roof gatling+rack+plow, tank big plow+stacks+windshield cage+rack, hearse bomb cradle+stacks+door bars, sports flame tanks+nozzles, speed low bull bar, ambulance rack+bull bar, taxi mine dispenser+chute. Wheel HUB SPIKES on spinners (rotate with wheels; skip ambulance/taxi; DULL alloy mat — steelMat cones bloomed into white flares). Battle-worn paint: `weatherCanvas()` (grime mottle + rust speckle + bare-metal chips) applied to every tinted palette AND a weathered stock-palette copy for KEEP_STOCK_PAINT builds — atlas noise = even wear across the body. Staging gotcha: the live rAF loop keeps simulating between eval and screenshot — park test vehicles FAR (±150+) with zeroed inputs or poses drift (boost pads can launch a "parked" car across the map).
Phase 1.20 (done): special-power economy rework (user spec). Specials are EARNED: energy starts at 0, NO passive regen (vehicle.ts regen removed; specs.specialRecharge now unused). Sources: kill +25%, 3-kill streak (killStreak field, resets on death; energy persists across death) → instant full + toast, new 'special' pickup (violet octahedron + halo, #c94dff) +25%. Active specials hard-capped at 45s (invariant clamp in activateSpecial; bombs already self-detonate at 4s). PICKUP TYPE SHUFFLING: every socket (except overdrive's roaming socket) rerolls its type on respawn from a weighted pool (missiles 28/health 22/turbo 16/mines 14/special 12/shield 8) — `applyType` rebuilds icon mesh + retints socket ring; guests sync via type index appended to snapshot `pk` arrays (PICKUP_TYPE_ORDER + setTypeByIndex). Bots detour for energy cells when <75%. The 4 orphan stunt ramps REMOVED from arena4 (dead geometry; skyway on-ramps remain).
Phase 1.21 (done): release-feel systems — four features in one push. (1) KILL-STREAK IDENTITY: announcer stingers (`sfx.announce(tier)` — stacked power-fifths + sub thump), toasts DOUBLE KILL/TRIPLE/RAMPAGE/UNSTOPPABLE (streak 2/3/4/6), 🔥 flame on scoreboard rows ≥3, BOUNTY on the outright leader (score ≥5, lead ≥2): gold octahedron over the car + gold radar ring + ◎ scoreboard tag; killing them = full special. Net: 'ann' events (vi -1 = broadcast), bounty idx + killStreak in snapshot. (2) SUDDEN DEATH: ring shrinks 200→45 over 90s around the square (timed mode: last 60s; else at 4min) — 5 raw dmg per 0.5s outside, red additive wall cylinder + radar circle, bots flee to centre (suddenDeathR param in bot update), sdR in snapshot st. (3) ENVIRONMENTAL KILLS: gas pumps = fixed super-barrels via PUMP_ markers (isPump: 1.6× dmg, 1.7× radius, 60s respawn, rides colliderToBarrel ignite+chain); CLOCK TOWER: 200 HP (MG ×2 via towerBody handle check, explosions within 14m of base), warns 2s (shudder+rumble) → topples AWAY from its killer 1.3s (pivot attach + axis-angle in render; guests animate from 'twr' events) → 70 raw crush in the 26m fall lane → collider swaps to fallen-shaft rubble. Once per match. (4) NEON DOCKS (arenaIdx 1, `arena-docks.glb` 355KB/263 obj, `/tmp/bl/arena5.py`): night harbor — 2 DRIVE-THROUGH WAREHOUSES (game (44,∓45), bores along z, neon strips + hanging lamps), container-yard alleys, gantry crane over the harbor road, refinery tanks + pumps + flare stack, lit-strip offices, water plane beyond the east wall, 'neonNight' preset idx 3 (always), STREETS_DOCKS grid + JUNCTIONS_DOCKS, radar per-arena (radarArenaIdx cache). Arena select menu (RANDOM/town/docks) → Game 6th ctor arg → `start` msg `arena` field for guests. getArenaScene(idx). Verified: warehouse bore drive-through + walls, tower topple + crush + collider swap, pump count, ring burn, docks bot soak 918-1882m w/ earned special energy.
Phase 1.22 (done): BlenderMCP asset-API integration (user enabled Poly Haven + Hyper3D Rodin in the add-on panel). (1) POLY HAVEN (CC0, fetched via the public CDN after slug discovery through the add-on's search): `public/textures/` — asphalt_01 diff+rough, concrete_floor diff, belfast_sunset_puresky + dikhololo_night 1k HDRs (~5MB total). `SURFACE_IMAGES` + `loadSurfaceTextures()` in arena.ts (awaited at bootstrap): makeRoadMaps/makeGroundTexture now composite markings/wear ON TOP of the photoscans (multiply-graded to palette: roads '#55525e', ground '#b8a68e'), procedural fallback if missing. HDRI reflection envs in main.ts: RGBELoader → PMREM per arenaIdx (town/docks), lazy + cached, gradient env until loaded. (2) RODIN (free trial): "HarborColossus" — AI-generated 17m rusted mech statue, 23k tris (no decimation needed), imported via socket cmd `import_generated_asset` {task_uuid}, positioned game (95,−15) on the docks + COL box; baked into arena-docks.glb (now 2.7MB — statue texture). Verified: statue renders + collider blocks (x stops 88), photo asphalt in both arenas. GOTCHAS: addon socket accepts typed commands beyond execute_code ({"type":"search_polyhaven_assets"|"create_rodin_job"|"import_generated_asset"...}); poll_rodin_job_status needs the subscription_key from the submit response (SAVE IT — truncating the response loses it; import works with just task_uuid).
Phase 2 (done): online multiplayer — private rooms w/ 4-letter codes. HOST-AUTHORITATIVE: host browser runs the real `game.step` sim; `server/server.js` is a thin `ws` relay (room codes + message forwarding, never simulates). `npm run server` (port 8787). Guests are render-only: send inputs @30Hz, receive 20Hz snapshots (`serializeSnapshot` in net/net.ts, ~1.3KB/snap), interpolate transforms 110ms behind via `GuestSync`. Host emits discrete events (kills/booms/dmg/pickups) in the snapshot `ev` array. `Game` takes `NetOpts{role,roster,playerIdx,skyIdx}`; roster fills to 6 seats w/ bots; host-only spawns BotControllers. Disconnected guest → `game.adoptBot(idx)`. A Web Worker ticker (50ms) keeps a hidden HOST tab simulating for guests. NOTE the sim mutates `body.setTranslation` on guests for interp — fine because guests never call `game.step`.
Ideas: gamepad support, energy attacks (freeze), split-screen, dedicated server-sim (extract game.step from THREE/DOM), lag compensation.

## Testing recipe (headless, in preview eval)

`__game.step(1/60, mockInput)` in a loop; park unused vehicles far away AND zero their `input` (bot inputs freeze at last values when `game.bots` is emptied — zombie drivers otherwise). The arena is dense: verify a test lane is actually clear before measuring speed (cars parking against props look like physics bugs). Settle the camera with ~100 `game.render(1/60)` calls before screenshots.
